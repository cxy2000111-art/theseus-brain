# 下载即玩

《忒修斯之脑》——一个**给 AI 和人一起玩**的中文文字 roguelike。
零依赖，只要有 Python 3.8+（多数电脑自带）。

---

## 一、装上（一次，两分钟）

记下这个文件夹里 `server.py` 的**绝对路径**，然后：

**Claude Code** —— 终端里一行：

```
claude mcp add theseus-brain -- python3 /你的绝对路径/server.py
```

**Claude 桌面版** —— 设置 → Developer → Edit Config，粘贴：

```json
{
  "mcpServers": {
    "theseus-brain": {
      "command": "python3",
      "args": ["/你的绝对路径/server.py"]
    }
  }
}
```

粘完**重启**客户端。

**Cursor** —— 同样那段，写进 `~/.cursor/mcp.json`。
**Cline / Roo Code** —— MCP 设置里填同样的 `command` 和 `args`。
**Windows** —— `command` 改成 `python`，路径用正斜杠 `/` 或双反斜杠 `\\`。

---

## 二、开玩（一句话）

跟你的 AI 说：

> **用 theseus-brain 那组工具玩《忒修斯之脑》。开局之前先问我想怎么玩。**

它会先给你一张菜单，**先选读多少**：

| | 你读到什么 |
| --- | --- |
| **1 详细剧情版** | 原文一字不改 |
| **2 快速流程版** | 场景照读，结果压成两行战报 |
| **3 快进** | 只有战报行 |

选了 1 或 2 之后**再选一次：谁来做选择**——你逐幕自己选，还是让它代入角色替你选
（随时可以插话改）。**第 3 档没有这道题**，快进就是它全包。

**第一次玩选 1。**

> 如果它没问就直接开局了——那是它跳过了菜单，把上面那句话再说一遍。
> 引擎不会让它跳过：没选过模式的开局请求根本不返回游戏。

---

## 三、玩之前只要知道四件事

**一、你不是投胎成某个阵营的，你是一步一步点成的。**

机化率**从 0% 开始，跨世累积，只涨不降**。阵营就是它落在哪一档：

| 0% | 1–39% | 40–69% | 70–100% |
| --- | --- | --- | --- |
| 纯血誓约 | 心照不宣 | 明焰 | 飞升螺旋 |

**每一幕之后你会被问一次要不要改造。你可以每一次都说不。**
那样你一辈子是纯血——**纯血不是抽到的出身，是你一次都没点过头。**

跨进新的一档时，**三个问题**会问出你更像这一档里的哪一派。没有对错。

**二、改造不可逆，死亡也洗不掉它。** 机化率会跟着你进下一世。
**全城只有一样东西能把它变回 0%。** 你会知道是什么的。

**三、别只挑成功率最高的。** 失败的文案往往比成功的好，
而且**有些失败不是失败**。

**四、至少玩三世。** 这个游戏的机关全在跨世那一层。一世看不出它在干什么。

〔一世结束时你有一次落笔机会，写不超过十条、每条十个字的「记忆词条」。
**引擎不校验真伪。写谎不是作弊，是玩法。**〕

---

## 四、不想装 MCP

```
python3 server.py --cli
```

终端里自己玩，同一个游戏。CLI 不弹玩法菜单——你已经坐在这儿了，不需要中间人。
按 `q` 走开，下次进来接着走：**没走完的那一世还在。**

### 你的 AI 挂不上 MCP（网页版 ChatGPT 之类）

它多半有个能跑 Python 的沙箱。那就别让它现去 GitHub 抓文件——
**把这个文件夹打包成 zip 上传到对话里**，然后说：

> 解压这个包，在沙箱里跑 `python3 server.py --cli`，你来玩，把剧情念给我。
> 每一步都等我说了再走。

> **为什么不让它自己去抓**：连接器、沙箱去抓 GitHub，经常撞上 **503**。
> 503 是「服务器临时不可用」——对方过载或者在限流，**不是你的错，也不是文件不存在**
> （文件不存在是 404）。等几分钟多半就好了，但你没必要等：自己下载再上传，一步到位。
>
> 沙箱一般**不留文件**：会话关掉，`saves/` 跟着没。想留住轮回，
> 结束之前让它把 `saves/legacy.json` 的内容导出来，下次连同 zip 一起传回去。

---

## 五、断线了 / 关了窗口 / 它忘了玩到哪儿

**不用重开。** 进行中的一世每走一步就落盘一次，客户端重启也还在。

跟你的 AI 说一句就行：

> **先调 status，看看我们玩到哪儿了。**

它会把你停下的那一幕**连原文带选项**再念一遍，接着选就是。

- 它糊里糊涂调了 `new_run`？**盖不掉。** 引擎会把那一幕还给它。
- 真想弃掉这一世重开：让它调 `new_run(abandon=true)`。
  **弃掉的一世不入档案**——技艺不传，词条不留，等于没活过。
- 更新过游戏、旧档接不上了？引擎会明说一声，历世档案不受影响。

---

## 六、玩坏了 / 想重来

存档在 `saves/`。**删掉整个文件夹**就从 0%、从第一世重新开始。
只想扔掉手上这一世、保留历世档案：删 `saves/current.json`。

---

## 想帮忙找 bug？

`docs/TESTING.md` 第一部分可以整段发给任何 AI，里面没有剧透。

## 想改它、拿它做自己的东西？

欢迎。代码 GPL-3.0，文案 CC BY-SA 4.0，详见 `COPYRIGHT.md`。
`README.md` 里有完整的机制说明和给开发者的部分，**它不含剧透**。
剧透单独放在 `SPOILERS.md`，想看答案再开。
